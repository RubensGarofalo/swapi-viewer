import { HideFieldPipe } from './hide-field.pipe';

describe('HideFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new HideFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
